---
phase: 02-monitoring
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/monitoring/sentry.ts
  - lib/offline/sync-processor.ts
  - app/login/page.tsx
  - components/auth/logout-button.tsx
autonomous: true

must_haves:
  truths:
    - "Every error includes sync context (isOnline, pendingMutationCount, lastSyncTime)"
    - "User ID and email are attached to errors for attribution"
    - "Sync metrics (success, failure, conflict counts) are emitted after each sync run"
    - "Metrics are trace-connected (emitted within sync execution context)"
  artifacts:
    - path: "lib/monitoring/sentry.ts"
      provides: "Sentry helper functions"
      exports: ["setSyncContext", "setSentryUser", "clearSentryUser", "trackSyncMetrics"]
    - path: "lib/offline/sync-processor.ts"
      provides: "Sync processor with metrics integration"
      contains: "trackSyncMetrics"
    - path: "app/login/page.tsx"
      provides: "Login page with Sentry user context"
      contains: "setSentryUser"
    - path: "components/auth/logout-button.tsx"
      provides: "Logout button with Sentry user clearing"
      contains: "clearSentryUser"
  key_links:
    - from: "lib/offline/sync-processor.ts"
      to: "lib/monitoring/sentry.ts"
      via: "import trackSyncMetrics"
      pattern: "import.*trackSyncMetrics.*from.*monitoring/sentry"
    - from: "lib/monitoring/sentry.ts"
      to: "@sentry/nextjs"
      via: "Sentry.metrics.count"
      pattern: "Sentry\\.metrics\\.count"
    - from: "app/login/page.tsx"
      to: "lib/monitoring/sentry.ts"
      via: "import setSentryUser"
      pattern: "import.*setSentryUser.*from.*monitoring/sentry"
    - from: "components/auth/logout-button.tsx"
      to: "lib/monitoring/sentry.ts"
      via: "import clearSentryUser"
      pattern: "import.*clearSentryUser.*from.*monitoring/sentry"
---

<objective>
Add sync context and metrics tracking to Sentry so every error includes offline status and sync operations are measured.

Purpose: Developers can debug production errors with full context (was user offline? how many pending mutations?) and track sync health over time (success rate, conflict frequency).

Output: Sentry helper functions integrated with sync processor and auth flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-monitoring/02-CONTEXT.md
@.planning/phases/02-monitoring/02-RESEARCH.md
@.planning/phases/02-monitoring/02-01-SUMMARY.md

# Existing files to modify
@lib/offline/sync-processor.ts
@lib/offline/mutation-queue.ts
@app/login/page.tsx
@components/auth/logout-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sentry helper module with context and metrics functions</name>
  <files>lib/monitoring/sentry.ts</files>
  <action>
Create `lib/monitoring/sentry.ts` with the following exports:

**setSyncContext()** - Async function that:
- Imports `getPendingMutationCount` from `@/lib/offline/mutation-queue`
- Gets pending count via `getPendingMutationCount()`
- Calls `Sentry.setContext("sync", { isOnline: navigator.onLine, pendingMutationCount, lastSyncTime })`
- lastSyncTime should be passed as parameter or default to null

**setSentryUser(user: { id: string; email: string })** - Function that:
- Calls `Sentry.setUser({ id: user.id, email: user.email })`

**clearSentryUser()** - Function that:
- Calls `Sentry.setUser(null)`

**trackSyncMetrics(progress: SyncProgress)** - Function that:
- Import `SyncProgress` type from `@/lib/offline/sync-processor`
- Calculate successCount = progress.current - progress.errors.length - progress.conflicts.length
- Emit `Sentry.metrics.count("sync.success_count", successCount)`
- Emit `Sentry.metrics.count("sync.failure_count", progress.errors.length)`
- Emit `Sentry.metrics.count("sync.conflict_count", progress.conflicts.length)`

**updateSyncContextOnNetworkChange()** - Function that:
- Adds event listeners for 'online' and 'offline' window events
- Calls `setSyncContext()` when network status changes
- Returns cleanup function to remove listeners

Mark file with 'use client' directive since it uses browser APIs (navigator.onLine, window events).

Import Sentry as: `import * as Sentry from "@sentry/nextjs"`
  </action>
  <verify>
- `ls lib/monitoring/sentry.ts` shows file exists
- `grep "setSyncContext" lib/monitoring/sentry.ts` shows function defined
- `grep "Sentry.metrics.count" lib/monitoring/sentry.ts` shows metrics calls
- `npm run typecheck` passes
  </verify>
  <done>
lib/monitoring/sentry.ts exists with:
- setSyncContext() that attaches isOnline, pendingMutationCount, lastSyncTime to Sentry context
- setSentryUser() and clearSentryUser() for user attribution
- trackSyncMetrics() that emits counter metrics for sync results
- updateSyncContextOnNetworkChange() for automatic context updates
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate metrics into sync processor</name>
  <files>lib/offline/sync-processor.ts</files>
  <action>
Modify `lib/offline/sync-processor.ts`:

1. Add import at top:
```typescript
import { trackSyncMetrics, setSyncContext } from '@/lib/monitoring/sentry';
```

2. In `processAllMutations()` function, after the final progress is determined (after the for loop, after setting `progress.lastSyncedAt`):
- Call `trackSyncMetrics(progress)` to emit Sentry metrics
- Call `setSyncContext(progress.lastSyncedAt)` to update context with latest sync time

The metrics should be emitted within the sync execution context so they're trace-connected. Add the calls right before returning the final progress object.

Do NOT wrap in try/catch - if Sentry fails, let it fail silently (Sentry SDK handles this). The sync operation should not fail because monitoring failed.
  </action>
  <verify>
- `grep "trackSyncMetrics" lib/offline/sync-processor.ts` shows import and usage
- `grep "setSyncContext" lib/offline/sync-processor.ts` shows context update
- `npm run typecheck` passes
- `npm run test` passes (existing tests should still work)
  </verify>
  <done>
sync-processor.ts:
- Imports trackSyncMetrics and setSyncContext from monitoring/sentry
- Emits sync metrics at end of processAllMutations
- Updates sync context with lastSyncTime after each sync run
- Existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire user context to login and logout</name>
  <files>app/login/page.tsx, components/auth/logout-button.tsx</files>
  <action>
This codebase does NOT have a centralized auth provider. Auth state changes happen directly in:
- `app/login/page.tsx` for login
- `components/auth/logout-button.tsx` for logout

**Modify app/login/page.tsx:**

1. Add import at top (after other imports):
```typescript
import { setSentryUser, setSyncContext, updateSyncContextOnNetworkChange } from '@/lib/monitoring/sentry';
```

2. In the `handleLogin` function, after successful login (after `if (!data.user)` check passes, before role checking), add:
```typescript
// Set Sentry user context for error attribution
setSentryUser({ id: data.user.id, email: data.user.email || '' });
// Initialize sync context
setSyncContext();
```

3. Add a useEffect hook to set up network change listener on component mount:
```typescript
useEffect(() => {
  const cleanup = updateSyncContextOnNetworkChange();
  return cleanup;
}, []);
```
This ensures sync context updates even while on the login page.

**Modify components/auth/logout-button.tsx:**

1. Add import at top:
```typescript
import { clearSentryUser } from '@/lib/monitoring/sentry';
```

2. In the `handleLogout` function, before calling `supabase.auth.signOut()`, add:
```typescript
// Clear Sentry user context
clearSentryUser();
```
  </action>
  <verify>
- `grep "setSentryUser" app/login/page.tsx` shows import and usage
- `grep "clearSentryUser" components/auth/logout-button.tsx` shows import and usage
- `npm run typecheck` passes
- `npm run lint` passes
  </verify>
  <done>
User context wired to auth:
- app/login/page.tsx calls setSentryUser after successful login with user id and email
- app/login/page.tsx calls setSyncContext to initialize sync context after login
- app/login/page.tsx sets up updateSyncContextOnNetworkChange on mount
- components/auth/logout-button.tsx calls clearSentryUser before signing out
  </done>
</task>

</tasks>

<verification>
Run all verification commands:
```bash
npm run typecheck && npm run lint && npm test
```

Verify helper functions exist:
```bash
grep -E "export (async )?function" lib/monitoring/sentry.ts
```

Verify integration:
```bash
grep "trackSyncMetrics" lib/offline/sync-processor.ts
grep "setSentryUser" app/login/page.tsx
grep "clearSentryUser" components/auth/logout-button.tsx
```
</verification>

<success_criteria>
1. lib/monitoring/sentry.ts exports setSyncContext, setSentryUser, clearSentryUser, trackSyncMetrics
2. sync-processor.ts emits sync metrics (success/failure/conflict counts) after each sync
3. sync-processor.ts updates sync context with lastSyncTime after sync
4. app/login/page.tsx sets Sentry user context after successful login
5. components/auth/logout-button.tsx clears Sentry user context on logout
6. Network change listener updates sync context automatically
7. All existing tests pass
8. Typecheck and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-monitoring/02-02-SUMMARY.md`
</output>
